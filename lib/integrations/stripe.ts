export async function lookupPaymentByEmail(email: string) {
  void email;
  return { matched: false, source: "stub" };
}

export async function lookupPaymentByName(firstName: string, lastName: string) {
  void firstName;
  void lastName;
  return { matched: false, source: "stub" };
}
