export async function searchSubmission(query: string) {
  void query;
  return { matched: false, source: "stub" };
}

export async function matchEmailOrName(email: string | null, fullName: string) {
  void email;
  void fullName;
  return { matched: false, source: "stub" };
}
